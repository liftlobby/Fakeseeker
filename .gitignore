# --- Python ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class$

# C extensions
*.so
*.pyd

# Distribution / packaging
.Python
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtual Environments ---
# Add the specific name(s) you use
venv/
.venv/
env/
.env/
ENV/
env.bak/
venv.bak/

# --- PyInstaller ---
# Log file
*.log
# Buid directory
build/
# Distribution directory
dist/
# Spec file
*.spec

# --- Project Specific ---
# Logs directory (created by logger_setup.py)
logs/

# Reports directory (created by appdirs logic)
# If you want to track an empty folder structure, add a .gitkeep file inside
# reports/ and reports/thumbnails/ and comment these lines out.
# Generally, generated reports/history are NOT tracked.
reports/scan_history.json
reports/thumbnails/

# checksum
checksum/

#dataset
dataset/

# User data directory (created by appdirs logic)
# Contains reports/, thumbnails/, models/, version.txt etc.
# Ensure this matches the 'user_data_dir' name if using the fallback
user_data/

# Downloaded models or training outputs (should be in user_model_dir or separate)
models/run_*/
models/best_model*.pth
models/final_model*.pth
# Don't commit downloaded models (they go in user_model_dir which should be ignored via user_data/ or reports/)
# optimal_threshold_*.json # Timestamped versions from training runs

# --- IDE / Editor Files ---
# VS Code
.vscode/
# PyCharm
.idea/
# Sublime Text
*.sublime-project
*.sublime-workspace
# Other
*.suo
*.ntvs*
*.njsproj
*.sln

# --- OS Files ---
# macOS
.DS_Store
.AppleDouble
.LSOverride
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
# Linux
*~
# --- Other ---
# Temporary files
*.tmp
*.bak
*.swp